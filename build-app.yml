---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: openjdk
    tag: '8'

inputs:
- name: resource-web-app
outputs:
- name: build

run:
  path: sh
  args:
  - -exc
  - |
    cd resource-web-app
    ./gradlew clean assemble
    cp -a build ..
