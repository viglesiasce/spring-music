---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: openjdk
    tag: '8'

inputs:
- name: resource-web-app

run:
  path: sh
  args:
  - -exc
  - |
    ./gradlew clean assemble
